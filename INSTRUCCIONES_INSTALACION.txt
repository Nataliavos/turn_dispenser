## 🧩 INSTRUCCIONES DE INSTALACIÓN – Proyecto `turn_dispenser`

### 1️⃣ Requisitos previos
Antes de ejecutar el proyecto, asegúrate de tener instalados los siguientes componentes:

- **Python 3.10 o superior**  
  Puedes verificar si está instalado ejecutando en PowerShell o CMD:
  ```bash
  python --version
  ```
  Si no está instalado, descárgalo desde:  
  👉 https://www.python.org/downloads/

- **Google Chrome o Microsoft Edge**  
  (Playwright puede usarlos para las pruebas automatizadas).

---

### 2️⃣ Crear el entorno virtual (venv)
1. Abre PowerShell en la carpeta del proyecto (por ejemplo `C:\Users\PC\Desktop\turn_dispenser`)
2. Ejecuta:

   ```bash
   python -m venv venv
   ```

   Esto crea una carpeta llamada `venv` que contendrá las dependencias del proyecto.

---

### 3️⃣ Activar el entorno virtual

#### Si estás en **Windows PowerShell**:
Antes de activarlo por primera vez, habilita la ejecución de scripts:

```bash
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
```

Luego, activa el entorno:

```bash
.\venv\Scripts\Activate.ps1
```

Deberías ver algo como:
```
(venv) PS C:\Users\PC\Desktop\turn_dispenser>
```

---

### 4️⃣ Instalar las dependencias necesarias

Con el entorno activado, ejecuta:

```bash
pip install playwright
```

Luego, instala los navegadores compatibles con Playwright:

```bash
playwright install
```

Esto descargará Chromium, Firefox y WebKit (usamos Chromium por defecto).

---

### 5️⃣ Verificar instalación

Ejecuta este comando para confirmar que Playwright funciona correctamente:

```bash
python -m playwright --version
```

Deberías ver un número de versión (por ejemplo `Playwright 1.48.0`).

---

### 6️⃣ Ejecutar el script de prueba

Con todo instalado, ejecuta el archivo principal:

```bash
python app_test_playwright.py
```

Esto:
- Abre el navegador Chrome/Chromium.
- Carga la página del RUNT.
- Deja el navegador abierto hasta que presiones ENTER en la consola.

---

### 7️⃣ Solución a posibles errores comunes

#### ❌ Error: “La ejecución de scripts está deshabilitada”
Vuelve a ejecutar:
```bash
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
```

#### ❌ Error: “playwright no se reconoce como comando”
Esto pasa si no se activó el entorno `venv`. Actívalo con:
```bash
.\venv\Scripts\Activate.ps1
```

#### ❌ Error al instalar navegadores
Ejecuta:
```bash
python -m playwright install
```

---

### 8️⃣ Archivos principales del proyecto

| Archivo | Descripción |
|----------|--------------|
| `app_test_playwright.py` | Script principal que abre el navegador y navega al portal del RUNT. |
| `venv/` | Entorno virtual (no se comparte, se crea en cada PC). |
| `INSTRUCCIONES_INSTALACION.txt` | Este archivo con pasos detallados. |

---

### 9️⃣ Opcional – Archivo de requerimientos

Puedes crear un archivo `requirements.txt` para instalar dependencias automáticamente:

Contenido del archivo:
```
playwright
```

Y luego ejecutar:
```bash
pip install -r requirements.txt
```

---

### ✅ Listo
Con estos pasos, podrás ejecutar el proyecto en cualquier otro computador con Windows fácilmente.

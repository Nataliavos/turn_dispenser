# Dependencias de runtime (solo paquetes usados directamente por la app).
# Las transitivas las resuelve pip; no congelar el árbol completo aquí.
PyQt6>=6.6.0
beautifulsoup4==4.14.2
playwright==1.55.0
